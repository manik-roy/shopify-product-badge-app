query Input {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
  }
  discountNode {
    metafield(
      namespace: "$app:total-order-discount"
      key: "function-configuration"
    ) {
      value
    }
  }
}
